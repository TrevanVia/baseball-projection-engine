{
  "name": "baseball-projection-engine",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && vercel --prod && vercel alias $(vercel ls --meta production=true | grep https | head -1 | awk '{print $2}') viacastbaseball.com"
  },
  "dependencies": {
    "@vercel/analytics": "^1.6.1",
    "claude": "^0.1.1",
    "lodash": "^4.17.21",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "recharts": "^2.12.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.4.0"
  }
}
